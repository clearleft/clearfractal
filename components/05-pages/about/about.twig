<div class="Background">{% include '@site-header' %}</div>

<main>

    <div class="Background">{% include '@masthead' %}</div>

    <div class="Background">{% include '@page-lead' %}</div>

    {% include '@team-grid' %}

    {% include '@banner' with {bannerCaption: 'Take a peek inside', bannerHeadline: 'Our Studio', bannerModifier: '', bannerBody: '', "bannerLink": ''} %}
    
    {% include '@image-gallery' %}

    {% include '@banner' %}

</main>

{% include '@site-footer' %}
