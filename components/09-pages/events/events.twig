
<div class="Background Background--grey">
    {% include '@site-header' %}
    {% include '@masthead' %}
</div>

<main id="main-content">

    {# Current events #}
    {% include '@event-current-grid' %}

    <div class="Background Background--grey">
        <div class="Container Container--constrained">
            {% include '@centred-header' with eventsSubHead %}             
            {% include '@stats' %}
            {% include '@quote-grid' %}

            {% if mailingList %}
            <div class="l-space-before--mega l-space-after--mega">
                {% set mailingList = mailingList|merge({inputGroupModifiers:'highlight'}) %}
                {% include '@input-group' with mailingList only %}
            </div>
            {% endif %}

        </div>
    </div>

    {% include '@hello' %}

    
</main>

{% include '@site-footer' with footer only %}
